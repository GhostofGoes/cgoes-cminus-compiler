bool a;
int b[100];

int max(int x)
{
    int z, zz;
    char c;

    if x>y) z=x 666;
    else z=y;

    f|each (x "dogs") c = x * 4 + 'z';
    
    while (666>) {
       break;

    z = & zz;

} 12
