// recursive factorial function
int factorial(int n)
{
        if (n<2) return 1;
        else return n*factorial(n-1);
}

main()
{
        int n;

        n = 10;
        n = factorial(n);
        output(n);
        outnl();
}

        
