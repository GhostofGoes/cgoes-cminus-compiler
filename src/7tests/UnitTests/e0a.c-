main()
{
    int x;

    x = 0;
    if (true) 
    while (x<10) {
        output(x);
        x = x+1;
    }
    else output(666);

    x = 0;
    if (false)
    while (x<10) {
        output(x);
        x = x+1;
    }
    else output(666);
}
