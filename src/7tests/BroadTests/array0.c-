int g[4];
int show(int z[]; int n)
{
        output(z[n]);
        return z[n];
}

main()
{
        int a[4];

        g[1] = 666;
        a[1] = 777;

        output(a[1]);
        output(g[1]);
        outnl();

        show(a, 1);
        show(g, 1);
        outnl();

        output(show(a, 1));
        output(show(g, 1));
        outnl();

        g[1]+=4;
        a[1]+=3;
        
        output(a[1]);
        output(g[1]);
        outnl();

        output(a[1]+=100);
        output(g[1]+=100);
        outnl();
}

